export const convertPropsToClasses = ({ disabled, }) => {
    // Start with base DaisyUI class
    let classes = 'file-input';
    // Disabled state
    if (disabled) {
        classes = `${classes} file-input-disabled`;
    }
    return classes;
};
