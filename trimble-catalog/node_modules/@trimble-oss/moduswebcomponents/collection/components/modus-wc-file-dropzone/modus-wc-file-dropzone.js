import { h, Host, } from "@stencil/core";
import { convertPropsToClasses } from "./modus-wc-file-dropzone.tailwind";
import { inheritAriaAttributes } from "../utils";
/**
 * File dropzone component that allows users to drag and drop files for upload.
 *
 * The component supports a `<slot>` called 'dropzone' for adding custom content such as progress indicators or additional instructions within the dropzone area.
 */
export class ModusWcFileDropzone {
    constructor() {
        this.inheritedAttributes = {};
        /** Tracks if files are being dragged over the dropzone */
        this.isDraggingOver = false;
        /** Tracks if file has any validation error and the type of error */
        this.invalidFile = 'none';
        /** Stores the error message when validation fails */
        this.errorMessage = '';
        /** Tracks if files were successfully uploaded */
        this.uploadSuccess = false;
        /** Custom CSS class to apply to the file dropzone element */
        this.customClass = '';
        /** Include state icon (upload, success, error) */
        this.includeStateIcon = true;
        this.handleDropzoneDrop = (event) => {
            var _a;
            event.preventDefault();
            event.stopPropagation();
            this.isDraggingOver = false;
            if (this.disabled)
                return;
            const files = (_a = event.dataTransfer) === null || _a === void 0 ? void 0 : _a.files;
            if (files && files.length > 0) {
                // Reset validation state
                this.invalidFile = 'none';
                this.errorMessage = '';
                this.uploadSuccess = false;
                // Check file count
                if (!this.isValidFileCount(files.length)) {
                    this.setInputValue('count');
                    this.errorMessage = this.getErrorMessage('count');
                    return;
                }
                // Check total file size
                if (!this.isValidFileSize(files)) {
                    this.setInputValue('size');
                    this.errorMessage = this.getErrorMessage('size');
                    return;
                }
                // Check each file
                for (let i = 0; i < files.length; i++) {
                    // Check file type
                    if (!this.isValidFileType(files[i])) {
                        this.setInputValue('type');
                        this.errorMessage = this.getErrorMessage('type');
                        return;
                    }
                    // Check file name length
                    if (!this.isValidFileName(files[i])) {
                        this.setInputValue('name');
                        this.errorMessage = this.getErrorMessage('name');
                        return;
                    }
                }
                this.uploadSuccess = true;
                this.fileSelect.emit(files);
            }
        };
        this.handleDropzoneDragOver = (event) => {
            event.preventDefault();
            event.stopPropagation();
            if (this.disabled)
                return;
            if (!this.isDraggingOver) {
                this.isDraggingOver = true;
            }
        };
        this.handleDropzoneDragLeave = (event) => {
            event.preventDefault();
            event.stopPropagation();
            this.isDraggingOver = false;
        };
        this.handleDropzoneClick = () => {
            var _a;
            if (this.disabled)
                return;
            (_a = this.inputRef) === null || _a === void 0 ? void 0 : _a.click();
        };
        this.handleDropzoneKeyDown = (event) => {
            var _a;
            if (event.key === ' ' || event.key === 'Enter') {
                event.preventDefault();
                if (this.disabled)
                    return;
                (_a = this.inputRef) === null || _a === void 0 ? void 0 : _a.click();
            }
        };
    }
    componentWillLoad() {
        this.inheritedAttributes = inheritAriaAttributes(this.el);
    }
    /**
     * Reset the dropzone to its initial state, clearing all error and success states
     */
    async reset() {
        this.invalidFile = 'none';
        this.errorMessage = '';
        this.uploadSuccess = false;
        this.isDraggingOver = false;
        if (this.inputRef) {
            this.inputRef.value = '';
        }
        return Promise.resolve();
    }
    // Generate error message based on validation type - called only when validation fails
    getErrorMessage(errorType) {
        switch (errorType) {
            case 'type':
                return this.invalidFileTypeMessage || 'File format not accepted';
            case 'name':
                return 'Filename exceeds maximum length';
            case 'count':
                return `Maximum number of files allowed is ${this.maxFileCount}`;
            case 'size':
                return `Total file size exceeds ${this.formatFileSize(this.maxTotalFileSizeBytes)}`;
            default:
                return 'Validation error';
        }
    }
    isValidFileType(file) {
        if (!this.acceptFileTypes)
            return true;
        const acceptedTypes = this.acceptFileTypes
            .split(',')
            .map((type) => type.trim().toLowerCase());
        const fileName = file.name.toLowerCase();
        const fileType = file.type.toLowerCase();
        return acceptedTypes.some((acceptedType) => {
            if (acceptedType.includes('/*')) {
                const mainType = acceptedType.split('/')[0];
                return fileType.startsWith(mainType + '/');
            }
            else if (acceptedType.startsWith('.')) {
                return fileName.endsWith(acceptedType);
            }
            else {
                return fileType === acceptedType;
            }
        });
    }
    isValidFileName(file) {
        if (this.maxFileNameLength === undefined || this.maxFileNameLength === null)
            return true;
        const fileNameWithoutExtension = file.name.replace(/\.[^/.]+$/, '');
        return fileNameWithoutExtension.length <= this.maxFileNameLength;
    }
    isValidFileCount(fileCount) {
        if (this.maxFileCount === undefined || this.maxFileCount === null)
            return true;
        return fileCount <= this.maxFileCount;
    }
    isValidFileSize(files) {
        if (this.maxTotalFileSizeBytes === undefined ||
            this.maxTotalFileSizeBytes === null)
            return true;
        let totalSize = 0;
        for (let i = 0; i < files.length; i++) {
            totalSize += files[i].size;
        }
        return totalSize <= this.maxTotalFileSizeBytes;
    }
    setInputValue(val, inputElement) {
        this.invalidFile = val;
        const input = inputElement || this.inputRef;
        if (input)
            input.value = '';
        return false;
    }
    handleFileChange(event) {
        const files = event.target.files;
        // Early return if no files
        if (!files || files.length === 0) {
            return;
        }
        // Reset validation state
        this.invalidFile = 'none';
        this.errorMessage = '';
        this.uploadSuccess = false;
        // Check file count
        if (!this.isValidFileCount(files.length)) {
            this.setInputValue('count', event.target);
            this.errorMessage = this.getErrorMessage('count');
            return;
        }
        // Check total file size
        if (!this.isValidFileSize(files)) {
            this.setInputValue('size', event.target);
            this.errorMessage = this.getErrorMessage('size');
            return;
        }
        // Check each file
        for (let i = 0; i < files.length; i++) {
            // Check file type
            if (!this.isValidFileType(files[i])) {
                this.setInputValue('type', event.target);
                this.errorMessage = this.getErrorMessage('type');
                return;
            }
            // Check file name length
            if (!this.isValidFileName(files[i])) {
                this.setInputValue('name', event.target);
                this.errorMessage = this.getErrorMessage('name');
                return;
            }
        }
        this.fileSelect.emit(files);
        this.uploadSuccess = true;
    }
    getClasses() {
        const classList = ['modus-wc-file-input'];
        const propClasses = convertPropsToClasses({
            disabled: this.disabled,
        });
        // The order CSS classes are added matters to CSS specificity
        if (propClasses)
            classList.push(propClasses);
        return classList.join(' ');
    }
    // Helper method to format file size in human-readable format
    formatFileSize(bytes) {
        if (!bytes)
            return '0 Bytes';
        const k = 1024;
        const sizes = ['Bytes', 'KB', 'MB', 'GB', 'TB'];
        const i = Math.floor(Math.log(bytes) / Math.log(k));
        return parseFloat((bytes / Math.pow(k, i)).toFixed(2)) + ' ' + sizes[i];
    }
    render() {
        const messages = {
            success: this.successMessage || 'Successfully uploaded',
            dragOver: this.fileDraggedOverInstructions || 'Drop files here',
        };
        const hasState = this.isDraggingOver || this.invalidFile !== 'none' || this.uploadSuccess;
        const showIcon = this.includeStateIcon !== false;
        const iconMap = {
            dragging: 'cloud_upload',
            invalid: 'alert',
            success: 'check_circle',
            default: 'cloud_upload',
        };
        const iconState = this.isDraggingOver
            ? iconMap.dragging
            : this.invalidFile !== 'none'
                ? iconMap.invalid
                : this.uploadSuccess
                    ? iconMap.success
                    : iconMap.default;
        const iconClass = this.isDraggingOver
            ? 'upload-icon'
            : this.invalidFile !== 'none'
                ? 'error-icon'
                : this.uploadSuccess
                    ? 'success-icon'
                    : 'upload-icon';
        const message = this.isDraggingOver
            ? messages.dragOver
            : this.invalidFile !== 'none'
                ? this.errorMessage
                : this.uploadSuccess
                    ? messages.success
                    : this.instructions;
        const showDropzoneSlot = hasState || this.disabled ? 'none' : 'block';
        return (h(Host, { key: 'a3544ba6e7ff39b4f8a9167ea82660375ae0d63a' }, h("div", { key: '515cda7e7fe2fb76a9bb481008f963ebdf26fbbe', class: "modus-wc-file-dropzone" }, h("input", { key: 'a1497bd2df31c9664cb0da138a6858e2dbe37026', accept: this.acceptFileTypes, class: this.getClasses(), disabled: this.disabled, multiple: this.multiple, onChange: (event) => this.handleFileChange(event), ref: (el) => (this.inputRef = el), type: "file" }), h("div", Object.assign({ key: 'e3b060a9c1c298deaa7bc060ff315dd27c3fa1c8', "aria-disabled": this.disabled ? 'true' : 'false', class: `dropzone-content 
              ${this.isDraggingOver ? 'dragging-over' : ''} 
              ${!this.isDraggingOver && this.invalidFile !== 'none' ? 'invalid-file-type' : ''} 
              ${!this.isDraggingOver && this.uploadSuccess ? 'upload-success' : ''} 
              ${this.customClass || ''}`, onClick: this.handleDropzoneClick, onDragLeave: this.handleDropzoneDragLeave, onDragOver: this.handleDropzoneDragOver, onDrop: this.handleDropzoneDrop, onKeyDown: this.handleDropzoneKeyDown, role: "button", tabindex: this.disabled ? -1 : 0 }, this.inheritedAttributes), h("div", { key: '88cf1c0e0f23f7b41212421fae52060a53c06e7e', class: "default-content" }, showIcon && (h("modus-wc-icon", { key: '56d4a0e6c09788a91f0856abcb9563f026ede41d', class: iconClass, name: iconState, size: "lg", variant: "solid" })), h("span", { key: 'dba970f82275830006922812d4dfb77354860f92' }, message), h("div", { key: '16784afa0aa1205675b8e62b7399b78c1d1de11b', style: { display: showDropzoneSlot } }, h("slot", { key: 'b4b7d67341913e533f7c2b855e523203efd7e2e6', name: "dropzone" })))))));
    }
    static get is() { return "modus-wc-file-dropzone"; }
    static get originalStyleUrls() {
        return {
            "$": ["modus-wc-file-dropzone.scss"]
        };
    }
    static get styleUrls() {
        return {
            "$": ["modus-wc-file-dropzone.css"]
        };
    }
    static get properties() {
        return {
            "acceptFileTypes": {
                "type": "string",
                "attribute": "accept-file-types",
                "mutable": false,
                "complexType": {
                    "original": "string",
                    "resolved": "string | undefined",
                    "references": {}
                },
                "required": false,
                "optional": true,
                "docs": {
                    "tags": [],
                    "text": "Accepted file types (e.g. '.jpg,.png' or 'image/*')"
                },
                "getter": false,
                "setter": false,
                "reflect": false
            },
            "customClass": {
                "type": "string",
                "attribute": "custom-class",
                "mutable": false,
                "complexType": {
                    "original": "string",
                    "resolved": "string | undefined",
                    "references": {}
                },
                "required": false,
                "optional": true,
                "docs": {
                    "tags": [],
                    "text": "Custom CSS class to apply to the file dropzone element"
                },
                "getter": false,
                "setter": false,
                "reflect": false,
                "defaultValue": "''"
            },
            "disabled": {
                "type": "boolean",
                "attribute": "disabled",
                "mutable": false,
                "complexType": {
                    "original": "boolean",
                    "resolved": "boolean | undefined",
                    "references": {}
                },
                "required": false,
                "optional": true,
                "docs": {
                    "tags": [],
                    "text": "Disable the file input"
                },
                "getter": false,
                "setter": false,
                "reflect": false
            },
            "fileDraggedOverInstructions": {
                "type": "string",
                "attribute": "file-dragged-over-instructions",
                "mutable": false,
                "complexType": {
                    "original": "string",
                    "resolved": "string | undefined",
                    "references": {}
                },
                "required": false,
                "optional": true,
                "docs": {
                    "tags": [],
                    "text": "Custom instructions shown when files are dragged over the dropzone"
                },
                "getter": false,
                "setter": false,
                "reflect": false
            },
            "includeStateIcon": {
                "type": "boolean",
                "attribute": "include-state-icon",
                "mutable": false,
                "complexType": {
                    "original": "boolean",
                    "resolved": "boolean | undefined",
                    "references": {}
                },
                "required": false,
                "optional": true,
                "docs": {
                    "tags": [],
                    "text": "Include state icon (upload, success, error)"
                },
                "getter": false,
                "setter": false,
                "reflect": false,
                "defaultValue": "true"
            },
            "instructions": {
                "type": "string",
                "attribute": "instructions",
                "mutable": false,
                "complexType": {
                    "original": "string",
                    "resolved": "string | undefined",
                    "references": {}
                },
                "required": false,
                "optional": true,
                "docs": {
                    "tags": [],
                    "text": "Custom instructions shown as the default dropzone message"
                },
                "getter": false,
                "setter": false,
                "reflect": false
            },
            "invalidFileTypeMessage": {
                "type": "string",
                "attribute": "invalid-file-type-message",
                "mutable": false,
                "complexType": {
                    "original": "string",
                    "resolved": "string | undefined",
                    "references": {}
                },
                "required": false,
                "optional": true,
                "docs": {
                    "tags": [],
                    "text": "Custom error message displayed when an invalid file type is selected"
                },
                "getter": false,
                "setter": false,
                "reflect": false
            },
            "maxFileNameLength": {
                "type": "number",
                "attribute": "max-file-name-length",
                "mutable": false,
                "complexType": {
                    "original": "number",
                    "resolved": "number | undefined",
                    "references": {}
                },
                "required": false,
                "optional": true,
                "docs": {
                    "tags": [],
                    "text": "Maximum allowed length of filename, will show error if exceeded"
                },
                "getter": false,
                "setter": false,
                "reflect": false
            },
            "maxFileCount": {
                "type": "number",
                "attribute": "max-file-count",
                "mutable": false,
                "complexType": {
                    "original": "number",
                    "resolved": "number | undefined",
                    "references": {}
                },
                "required": false,
                "optional": true,
                "docs": {
                    "tags": [],
                    "text": "Maximum number of files allowed, will show error if exceeded"
                },
                "getter": false,
                "setter": false,
                "reflect": false
            },
            "maxTotalFileSizeBytes": {
                "type": "number",
                "attribute": "max-total-file-size-bytes",
                "mutable": false,
                "complexType": {
                    "original": "number",
                    "resolved": "number | undefined",
                    "references": {}
                },
                "required": false,
                "optional": true,
                "docs": {
                    "tags": [],
                    "text": "Maximum total file size in bytes allowed, will show error if exceeded"
                },
                "getter": false,
                "setter": false,
                "reflect": false
            },
            "multiple": {
                "type": "boolean",
                "attribute": "multiple",
                "mutable": false,
                "complexType": {
                    "original": "boolean",
                    "resolved": "boolean | undefined",
                    "references": {}
                },
                "required": false,
                "optional": true,
                "docs": {
                    "tags": [],
                    "text": "Allow multiple file selection"
                },
                "getter": false,
                "setter": false,
                "reflect": false
            },
            "successMessage": {
                "type": "string",
                "attribute": "success-message",
                "mutable": false,
                "complexType": {
                    "original": "string",
                    "resolved": "string | undefined",
                    "references": {}
                },
                "required": false,
                "optional": true,
                "docs": {
                    "tags": [],
                    "text": "Success message displayed when files are uploaded successfully"
                },
                "getter": false,
                "setter": false,
                "reflect": false
            }
        };
    }
    static get states() {
        return {
            "isDraggingOver": {},
            "invalidFile": {},
            "errorMessage": {},
            "uploadSuccess": {}
        };
    }
    static get events() {
        return [{
                "method": "fileSelect",
                "name": "fileSelect",
                "bubbles": true,
                "cancelable": true,
                "composed": true,
                "docs": {
                    "tags": [],
                    "text": "Event emitted when files are selected"
                },
                "complexType": {
                    "original": "FileList",
                    "resolved": "FileList",
                    "references": {
                        "FileList": {
                            "location": "global",
                            "id": "global::FileList"
                        }
                    }
                }
            }];
    }
    static get methods() {
        return {
            "reset": {
                "complexType": {
                    "signature": "() => Promise<void>",
                    "parameters": [],
                    "references": {
                        "Promise": {
                            "location": "global",
                            "id": "global::Promise"
                        }
                    },
                    "return": "Promise<void>"
                },
                "docs": {
                    "text": "Reset the dropzone to its initial state, clearing all error and success states",
                    "tags": []
                }
            }
        };
    }
    static get elementRef() { return "el"; }
}
