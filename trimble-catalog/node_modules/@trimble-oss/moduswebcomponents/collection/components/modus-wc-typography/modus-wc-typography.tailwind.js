export const convertPropsToClasses = ({ size, weight, }) => {
    let classes = '';
    if (size) {
        classes = `${classes} modus-wc-text-${size}`;
    }
    if (weight) {
        classes = `${classes} modus-wc-typography-weight-${weight}`;
    }
    return classes.trim();
};
