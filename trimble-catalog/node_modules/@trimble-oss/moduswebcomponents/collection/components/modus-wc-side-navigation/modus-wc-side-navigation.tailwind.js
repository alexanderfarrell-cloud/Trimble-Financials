export const convertPropsToClasses = ({ expanded, }) => {
    let classes = '';
    if (expanded) {
        classes = `${classes} modus-wc-side-navigation-expanded`;
    }
    return classes.trim();
};
