import { ModusSize } from '../types';
import { ModusWcRatingVariant } from './modus-wc-rating';
export declare const convertPropsToClasses: ({ allowHalf, size, variant, }: {
    allowHalf?: boolean;
    size?: ModusSize;
    variant?: ModusWcRatingVariant;
}) => {
    ratingPropClasses: string;
    ratingItemPropClasses: string;
};
/**
 * Get the appropriate CSS class for a rating item based on variant and index
 */
export declare const getIndexedRatingItemClass: (index: number, baseClasses: string, showHalf: boolean, variant: ModusWcRatingVariant, count: number) => string;
