import { Density } from '../types';
interface TableProps {
    density?: Density;
    zebra?: boolean;
    hover?: boolean;
}
/**
 * Converts table props to CSS classes for styling
 * @param props Table props
 * @returns CSS class string
 */
export declare function convertTablePropsToClasses(props: TableProps): string;
export declare const tableSortableClasses: {
    header: {
        sortable: string;
        sorted: string;
        asc: string;
        desc: string;
    };
    icon: {
        base: string;
        asc: string;
        desc: string;
        none: string;
    };
};
export declare const convertRowPropsToClasses: ({ active, hover, }: {
    active?: boolean;
    hover?: boolean;
}) => string;
export declare const convertHeaderCellPropsToClasses: ({ sortable, }: {
    sortable?: boolean;
}) => string;
export {};
