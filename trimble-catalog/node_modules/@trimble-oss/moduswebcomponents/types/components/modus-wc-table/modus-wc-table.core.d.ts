import { ColumnDef, PaginationState, RowSelectionState, SortingState, Table as TanStackTable, Updater } from '@tanstack/table-core';
export interface Table<TData extends Record<string, unknown>> extends TanStackTable<TData> {
    setData: (data: TData[]) => void;
    setColumns: (columns: ColumnDef<TData, unknown>[]) => void;
}
export type { SortingState, PaginationState, ColumnDef };
/**
 * Creates a configured TanStack table instance with enhanced functionality
 */
export declare function createModusTable<TData extends Record<string, unknown>>(options: {
    data: TData[];
    columns: ColumnDef<TData, unknown>[];
    pageCount?: number;
    sorting?: SortingState;
    pagination?: PaginationState;
    enableSorting?: boolean;
    manualSorting?: boolean;
    manualPagination?: boolean;
    enableRowSelection?: boolean;
    rowSelection?: RowSelectionState;
    onRowSelectionChange?: (updater: Updater<RowSelectionState>) => void;
    getRowId?: (originalRow: TData, index: number, parent?: unknown) => string;
    onSortingChange?: (updater: Updater<SortingState>) => void;
    onPaginationChange?: (updater: Updater<PaginationState>) => void;
}): Table<TData>;
/**
 * Transforms our column format to TanStack column definition
 */
export declare function transformColumns<TData extends Record<string, unknown>>(columns: Array<{
    id: string;
    accessor: string;
    header: string | HTMLElement;
    sortable?: boolean;
    className?: string;
    width?: string;
    cellRenderer?: (value: unknown, row: unknown) => string | HTMLElement;
}>, enableSorting?: boolean): ColumnDef<TData, unknown>[];
