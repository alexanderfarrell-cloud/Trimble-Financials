import { ModusSize } from '../types';
export declare const convertPropsToClasses: ({ active, disabled, hasError, shape, size, variant, }: {
    active?: boolean;
    disabled?: boolean;
    hasError?: boolean;
    shape?: "rectangle" | "circle";
    size?: ModusSize;
    variant?: "filled" | "outline";
}) => string;
