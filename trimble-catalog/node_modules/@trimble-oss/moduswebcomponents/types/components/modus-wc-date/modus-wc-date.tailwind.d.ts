import { DaisySize, IInputFeedbackProp } from '../types';
export declare const convertPropsToClasses: ({ bordered, feedback, readOnly, size, }: {
    bordered?: boolean;
    feedback?: IInputFeedbackProp;
    readOnly?: boolean;
    size?: DaisySize;
}) => string;
