import { ModusSize } from '../types';
export declare const convertPropsToClasses: ({ active, bordered, disabled, focused, size, }: {
    active?: boolean;
    bordered?: boolean;
    disabled?: boolean;
    focused?: boolean;
    size?: ModusSize;
}) => string;
