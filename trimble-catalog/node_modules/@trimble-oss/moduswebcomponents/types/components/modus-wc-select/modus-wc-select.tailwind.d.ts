import { IInputFeedbackProp, ModusSize } from '../types';
export declare const convertPropsToClasses: ({ bordered, feedback, size, }: {
    bordered?: boolean;
    feedback?: IInputFeedbackProp;
    size?: ModusSize;
}) => string;
