name: Deploy main to GitHub Pages

on:
  push:
    branches:
      - master
    paths:
      - 'financials-hub/**'
      - 'mockups-hub/**'
      - '.github/workflows/deploy-main.yml'
  workflow_dispatch:

concurrency:
  group: pages-main
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"
          cache-dependency-path: |
            financials-hub/package-lock.json
            mockups-hub/package-lock.json

      - name: Install Dependencies (financials-hub)
        working-directory: financials-hub
        run: npm ci

      - name: Build (financials-hub)
        working-directory: financials-hub
        run: npm run build -- --base=/Trimble-Financials/

      - name: Install Dependencies (mockups-hub)
        working-directory: mockups-hub
        run: npm ci

      - name: Build (mockups-hub)
        working-directory: mockups-hub
        run: npm run build -- --base=/Trimble-Financials/mockups/

      - name: Checkout gh-pages
        uses: actions/checkout@v4
        with:
          ref: gh-pages
          path: gh-pages
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Copy builds to gh-pages
        run: |
          find gh-pages -mindepth 1 -maxdepth 1 ! -name '.git' ! -name 'branches' -exec rm -rf {} +
          cp -r financials-hub/dist/. gh-pages/
          mkdir -p gh-pages/mockups
          cp -r mockups-hub/dist/. gh-pages/mockups/

      - name: Push to gh-pages
        run: |
          cd gh-pages
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add -A
          git diff --staged --quiet || git commit -m "Deploy master to GitHub Pages"
          git pull --rebase origin gh-pages
          git push origin gh-pages
